# 🚀 Render Auto-Deploy Guide

## ✅ Всё готово к автоматическому деплою!

Этот проект можно деплоить на Render БЕЗ ручных настроек!

---

## 📋 Что включено в репозиторий:

- ✅ **render.yaml** - конфигурация Render с переменными
- ✅ **.env.render** - все переменные окружения готовы
- ✅ **storage/jobs.json** - 500 предзаполненных задач
- ✅ **Dockerfile** - готовый к деплою

---

## 🎯 Как деплоить (ОЧЕНЬ ПРОСТО):

### Вариант 1: Через Render Dashboard (РЕКОМЕНДУЕМО)

1. Откройте https://render.com
2. Нажмите **"New +"** → **"Web Service"**
3. Выберите **"Deploy from Git Repository"**
4. Подключите GitHub: `evagev2016-pixel/test`
5. Нажмите **"Connect"**

Render АВТОМАТИЧЕСКИ:
- Прочитает `render.yaml`
- Установит все переменные из `.env.render`
- Загрузит задачи из `storage/`
- Запустит воркер

**ВСЁ РАБОТАЕТ БЕЗ РУЧНЫХ НАСТРОЕК!** ✨

---

### Вариант 2: Через CLI (если нужен более быстрый деплой)

```bash
npm install -g render-cli
render deploy
```

---

## 🔧 Если нужно изменить параметры:

Отредактируйте эти файлы и сделайте `git push`:

1. **Количество ботов:** `.env.render` → `TOTAL_BOTS=1000`
2. **Параметры прокси:** `.env.render` → `BRIGHTDATA_*`
3. **Ваш блог:** `.env.render` → `BLOG_HOMEPAGE_URL=https://your-blog.com`

После push в GitHub → Render автоматически перестраивает контейнер!

---

## 📊 Проверить статус деплоя:

1. Dashboard Render → Your Service
2. Вкладка **"Logs"** - смотрите процесс:

```
✅ Building...
✅ Installing dependencies...
✅ Installing browsers...
✅ Worker started
💡 Waiting for jobs...
✅ Processing job 1...
```

---

## 🎉 Результат:

- **500 задач** загружены из `storage/`
- **Все переменные** установлены из `.env.render`
- **Worker работает 24/7** в облаке
- **Ноль ручных настроек** нужно!

Просто push → Render делает всё автоматически! 🚀
