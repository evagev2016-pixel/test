# 🔐 Fix GitHub Access Issue

## Проблема
`Permission denied to gexamananyan-create` - неправильные учетные данные

## ✅ Решение

### Вариант 1: Использовать GitHub Personal Access Token

1. **Создайте токен на GitHub:**
   - Перейдите: https://github.com/settings/tokens
   - Нажмите: "Generate new token (classic)"
   - Выберите scope: `repo` (полный доступ к репозиториям)
   - Скопируйте токен

2. **Используйте токен при пушу:**
   ```bash
   git push -u origin main
   ```
   Когда попросит пароль - вставьте токен вместо пароля

### Вариант 2: Использовать SSH ключи

1. **Проверьте SSH ключи:**
   ```bash
   ls ~/.ssh/
   ```

2. **Если нет - создайте:**
   ```bash
   ssh-keygen -t ed25519 -C "evagev2016-pixel@github.com"
   # Нажмите Enter для дефолтного пути и пустого пароля
   ```

3. **Добавьте публичный ключ в GitHub:**
   - https://github.com/settings/keys
   - Нажмите "New SSH key"
   - Скопируйте содержимое файла `~/.ssh/id_ed25519.pub`
   - Вставьте в GitHub

4. **Переключитесь на SSH:**
   ```bash
   cd c:\Users\Nemesis\Desktop\Projects\adsterra-main\adsterra-main
   git remote set-url origin git@github.com:evagev2016-pixel/test.git
   git push -u origin main
   ```

### Вариант 3: Быстро через Visual Studio Code

1. В VS Code нажмите **Ctrl+Shift+G** (Source Control)
2. Нажмите **"..."** → **"Push"**
3. VS Code автоматически поможет с аутентификацией

## ✨ После успешного пуша:

1. Откройте https://railway.app
2. Нажмите **"Create New Project"**
3. Выберите **"Deploy from GitHub"** 
4. Выберите репозиторий `evagev2016-pixel/test`
5. Railway автоматически начнет деплой!

Приложение запустится в облаке за 3-5 минут.
