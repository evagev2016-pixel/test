# Proxy Provider Selection: 'iproyal', 'dataimpulse', or 'brightdata'
PROXY_PROVIDER=brightdata


# BrightData Mobile Proxy Configuration (if using BrightData)
BRIGHTDATA_HOST=brd.superproxy.io
BRIGHTDATA_PORT=33335
BRIGHTDATA_USERNAME=brd-customer-hl_d4382b99-zone-mb
BRIGHTDATA_PASSWORD=ql1bol9csls1
BRIGHTDATA_ZONE=mb
PROXY_COUNTRY=us  # Country code for BrightData proxy: us, uk, ca, fr, es, ie, au, cn

# Blog Configuration
BLOG_HOMEPAGE_URL=https://yoursite.com
SMART_LINK_TEXT=Click here to make money with sport betting

# Bot Configuration
TOTAL_BOTS=16000
SESSIONS_PER_BOT=10
TARGET_IMPRESSIONS=1500

# Browser Configuration
BROWSER_HEADLESS=true
BROWSER_TIMEOUT=30000

# Queue Configuration (DynamoDB-based)
DYNAMODB_ADSTERRA_JOBS_TABLE=AdsterraJobs
QUEUE_POLL_INTERVAL=1000
MAX_RETRIES=3

# Timing Configuration
MIN_SCROLL_WAIT=0
MAX_SCROLL_WAIT=0
MIN_AD_WAIT=20000
MAX_AD_WAIT=60000

# Worker Configuration
PROCESS_IMMEDIATELY=true # Set to true to process jobs immediately (ignore scheduled times)
MIN_CONCURRENT_JOBS=2 # Minimum concurrent browser sessions (default: 2)
CONCURRENT_JOBS=50 # Fallback number of jobs (worker auto-calculates from active runs)
MAX_CONCURRENT_JOBS=500 # Maximum concurrent jobs allowed (default: 500 for $500/day in 24h)
# Note: Each browser session uses ~50-100 MB RAM
# Example: 500 concurrent = ~25-50 GB RAM needed
# Adjust based on your instance size (DigitalOcean/EC2)

